<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:background="#F5F5F5">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp"
        android:gravity="center">

        <!-- Logo/Icon Container -->
        <ImageView
            android:id="@+id/ivScanLogo"
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:src="@drawable/ic_camara"
            android:contentDescription="Icona Escàner"
            android:layout_marginBottom="24dp"
            app:tint="#2196F3" />

        <!-- Title -->
        <TextView
            android:id="@+id/tvScanTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Escanejar Codi QR"
            android:textSize="24sp"
            android:textStyle="bold"
            android:textColor="#333333"
            android:layout_marginBottom="8dp" />

        <!-- Description -->
        <TextView
            android:id="@+id/tvScanDescription"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Enquadra el codi QR d'una ordre (de l'albarà o de la pantalla web) per obrir els seus detalls i poder gestionar-la."
            android:textSize="14sp"
            android:textColor="#666666"
            android:gravity="center"
            android:layout_marginBottom="32dp"
            android:paddingHorizontal="16dp" />

        <!-- Glassmorphism Card Container -->
        <com.google.android.material.card.MaterialCardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="32dp"
            app:cardCornerRadius="16dp"
            app:cardElevation="4dp"
            app:strokeWidth="1dp"
            app:strokeColor="#E0E0E0">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="24dp"
                android:gravity="center">

                <TextView
                    android:id="@+id/tvScanStatus"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Llest per escanejar"
                    android:textSize="16sp"
                    android:textStyle="bold"
                    android:textColor="#2196F3"
                    android:layout_marginBottom="16dp" />

                <!-- Spinner for visual loading indication -->
                <ProgressBar
                    android:id="@+id/pbScanLoading"
                    android:layout_width="40dp"
                    android:layout_height="40dp"
                    android:visibility="gone"
                    android:layout_marginBottom="16dp" />

                <!-- Action Button -->
                <Button
                    android:id="@+id/btnOpenScanner"
                    android:layout_width="match_parent"
                    android:layout_height="60dp"
                    android:text="OBRIR CÀMERA"
                    android:textSize="16sp"
                    android:textStyle="bold"
                    android:textColor="@android:color/white"
                    android:backgroundTint="#2196F3"
                    app:cornerRadius="12dp" />

            </LinearLayout>
        </com.google.android.material.card.MaterialCardView>

        <!-- Manual fallback text input just in case -->
        <TextView
            android:id="@+id/tvScanManualText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="- O escriu el codi manualment -"
            android:textColor="#888888"
            android:layout_marginBottom="12dp" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="24dp">

            <EditText
                android:id="@+id/etManualOrderId"
                android:layout_width="0dp"
                android:layout_height="55dp"
                android:layout_weight="1"
                android:hint="ID o Referència"
                android:inputType="textCapCharacters"
                android:background="@drawable/bg_rounded_white"
                android:paddingHorizontal="16dp"
                android:layout_marginEnd="8dp"
                android:elevation="1dp"
                android:importantForAutofill="no" />

            <ImageButton
                android:id="@+id/btnSearchManual"
                android:layout_width="55dp"
                android:layout_height="55dp"
                android:src="@android:drawable/ic_menu_search"
                android:backgroundTint="#4CAF50"
                android:tint="@android:color/white"
                android:elevation="1dp"
                android:contentDescription="Cerca manual" />

        </LinearLayout>

    </LinearLayout>
</ScrollView>
